name: Sync homarr to Aliyun ACR

on:
  workflow_dispatch:
  schedule:
    - cron: "27 18 * * *"

permissions:
  contents: read

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Install Skopeo
        run: |
          sudo apt-get update
          sudo apt-get install --yes skopeo

      - name: Log in to Aliyun ACR
        env:
          ACR_USERNAME: ${{ secrets.ACR_USERNAME }}
          ACR_PASSWORD: ${{ secrets.ACR_PASSWORD }}
        run: |
          echo "$ACR_PASSWORD" |
            skopeo login \
              --username "$ACR_USERNAME" \
              --password-stdin registry.cn-shanghai.aliyuncs.com

      - name: Copy all image architectures
        run: |
          skopeo copy --all \
            docker://docker.io/homarr-labs/homarr:latest \
            docker://registry.cn-shanghai.aliyuncs.com/rayless/homarr:latest
